using UnityEngine;

// ✨ 이 스크립트 파일이 없거나, 클래스 이름이 달라서 에러가 발생한 것입니다.
public static class SceneData
{
    public static Vector2 nextPlayerPosition;
    public static bool hasNextPlayerPosition = false;
}